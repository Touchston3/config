unbind C-b
unbind '"'
unbind %

set-option -g prefix C-a
bind-key C-a send-prefix
set -g default-terminal "$TERM"
set -ag terminal-overrides ",$TERM:Tc"

set -s escape-time 0
set -g base-index 1
set -g pane-base-index 1
set-option -g renumber-windows on
set-window-option -g pane-base-index 1


bind '-' split-window -v
bind "|" split-window -h
bind "n" next-window
bind "p" previous-window

set -g @plugin 'tmux-plugins/tpm'
##set -g @plugin 'catppuccin/tmux'
set -g @plugin 'Nybkox/tmux-kanagawa'
set -g @plugin 'christoomey/vim-tmux-navigator'

set -g mouse on
unbind -T copy-mode-vi MouseDragEnd1Pane
set-window-option -g pane-border-status off

set-option -g status-position bottom
set -g @kanagawa-theme 'dragon'


#set -g @catppuccin_window_left_separator ""
#et -g @catppuccin_window_right_separator " "
#et -g @catppuccin_window_middle_separator " █"
#et -g @catppuccin_window_number_position "right"
#
#et -g @catppuccin_window_default_fill "number"
#et -g @catppuccin_window_default_text "#W"
#
#et -g @catppuccin_window_current_fill "number"
#et -g @catppuccin_window_current_text "#W"
#
#et -g @catppuccin_status_modules_right "directory session"
#et -g @catppuccin_status_left_separator  " "
#et -g @catppuccin_status_right_separator ""
#et -g @catppuccin_status_fill "icon"
#et -g @catppuccin_status_connect_separator "no"
#
#et -g @catppuccin_directory_text "#{pane_current_path}"
#set -g @catppuccin_flavour "mocha"


run '~/.tmux/plugins/tpm/tpm'
